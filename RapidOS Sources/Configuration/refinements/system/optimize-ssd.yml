---
title: Set SSD Configuration
description: > 
  This script optimizes settings specifically for SSDs to enhance their 
  performance. HDDs are not affected by this optimization.
actions:
  - !powerShell:
      command: '.\MultiTool.ps1 -MyArgument configure_ssd'
      exeDir: true
      weight: 100